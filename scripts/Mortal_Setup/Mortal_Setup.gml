#region Doc
/// @function Mortal_Setup(num_maxHealth)
/// @param {number} num_maxHealth Max Health
#endregion
var _num_maxHealth = argument[0];


Collidable_Setup();

num_maxHealth = _num_maxHealth;
num_currentHealth = _num_maxHealth;