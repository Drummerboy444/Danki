/// @function Shake_RESET(id_oShake) Resets the given caller, if it is an oShake, adding it back to the pool.
/// @param {id} oShake object to be reset.

with(argument[0]){
	num_Duration = 0;
	num_strength = 0;
	scr_type = Shake_Basic;
}