/// @function Camera_ApplyOffset(num_xOffset, num_yOffset) Causes the camera to displace from its target by the 
///		given values.
/// @param {num} X offset.
/// @param {num} Y offset.

x += argument[0];
y += argument[1];
